
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hu.oneclick.dao.VersionMappingDao">

    <select id="selectByReleaseId" resultType="com.hu.oneclick.model.entity.VersionMapping">
        SELECT * FROM version_mapping 
        WHERE release_id = #{releaseId}
        ORDER BY env, env_version
    </select>

    <select id="selectByProjectAndRelease" resultType="com.hu.oneclick.model.entity.VersionMapping">
        SELECT * FROM version_mapping 
        WHERE project_id = #{projectId} 
        AND release_version = #{releaseVersion}
        ORDER BY env, env_version
    </select>

</mapper>
